export enum DaySection {
    EarlyMorning,
    Morning,
    Afternoon,
    Night,
}

export enum Paths {
    Home = '/',
    CalendarAPI = '/calendarAPI',
    Upload = '/upload',
    Yahrzeit = '/yahrzeit'
}